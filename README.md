When running this code, for example, add the argument "--deck nc_input.csv" in order to run the main file "nc_htgr.py". The inputs are changed through "nc_input.csv".

The axial heating profile is loaded from neutronics data in "neutronics.csv". The code automatically pulls "neutronics.csv" as long as they are in the same directory level.

yeah, that's about it.
